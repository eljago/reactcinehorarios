'use strict';

import React, { PropTypes } from 'react';

import ComponentVideosWebView from './ComponentVideosWebView';

export default class ContainerVideos extends React.Component {

  constructor(props) {
    super(props);
  }

  render() {
    return (
      <ComponentVideosWebView

      />
    );
  }
}