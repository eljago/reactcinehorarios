'use strict';

var React = require('react-native');
var {
  PixelRatio,
  StyleSheet,
} = React;

var colors = require('../../Data/colors');

module.exports = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 12
  }
});
